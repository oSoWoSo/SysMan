meta:
  code: en
  name: English

strings:
  app.title: VMman
  app.subtitle: Viewer Manager
  app.window: VMman - Viewer Manager
  tab.name: VMs

  filter.all: all
  filter.running: running
  filter.stopped: stopped

  search.placeholder: Search VMs...
  search.active: (press Enter to search, Esc to cancel)'
  search.clear: (Esc to clear)
  search.hint: Press / to search

  stats.fmt: "%d running / %d total (%d shown)"

  vms.header: Virtual Machines
  vms.none: No VMs found

  state.running: Running
  state.stopped: Stopped

  action.boot: Boot VM
  action.connect: Connect to VM

  detail.header: VM Details
  detail.name: Name
  detail.state: State
  detail.pid: PID
  detail.spice: SPICE
  detail.ssh: SSH
  detail.empty: "--"

  status.boot: VM started
  status.kill: VM stopped
  status.connected: Connected to VM
  status.err: Error
  status.created: "VM %s created"
  status.saved: "Saved config %s"

  new.title: New VM
  new.name: Name
  new.name_ph: e.g. dev-machine
  new.guest_os: Guest OS
  new.guest_os_ph: linux
  new.iso: ISO image path
  new.iso_ph: /path/to/isos/foo.iso
  new.memory: Memory (MB)
  new.cores: CPU cores
  new.ssh_user: SSH user
  new.ssh_user_ph: guest login (empty = current user)
  new.err_name: "Invalid VM name (use letters, digits, dot, dash, underscore)"
  new.err_exists: "VM %s already exists"

  edit.title: "Edit config: %s"

  log.title: "VM log: %s"
  log.empty: No log output yet
  log.hint: (esc/q to close)

  ssh_user.title: "SSH user for %s"
  ssh_user.empty: guest login (empty = current OS user)

  confirm.title: Confirm
  confirm.kill: "Kill VM %s?"
  confirm.hint: "y = confirm, any key = cancel"

  help.normal: "↑↓=nav  Enter=toggle  c=connect  b=boot  k=kill  /=search  tab=filter  q=quit"
  help.search: "Enter/esc=close  ↑↓=nav"

  btn.boot: Boot
  btn.kill: Kill
  btn.connect: Connect
  btn.new: New
  btn.edit: Edit
  btn.log: Log
  btn.ssh_user: SSH user
  btn.create: Create
  btn.save: Save
  btn.reload: Reload
  btn.about: About
  btn.close: Close

  about.version: "Version"
  about.author: "Author"
  about.license: "License"
  about.hint: "Press any key to go back"

  action.quit: "Quit"

  tooltip.vmsman.boot: Boot selected VM
  tooltip.vmsman.kill: Stop/Kill the VM
  tooltip.vmsman.connect: Connect to VM via SPICE
  tooltip.vmsman.new: Create a new VM
  tooltip.vmsman.edit: Edit the VM config file
  tooltip.vmsman.log: Show the VM boot log
  tooltip.vmsman.ssh_user: Set the SSH login user for this VM
  tooltip.vmsman.about: About this application
  tooltip.vmsman.reload: Reload VM list
  tooltip.vmsman.filter: Filter VMs
  tooltip.vmsman.search: Search VMs
  tooltip.vmsman.filter_all: Show all VMs
  tooltip.vmsman.filter_running: Show running VMs only
  tooltip.vmsman.filter_stopped: Show stopped VMs only
  tooltip.vmsman.settings: "Open settings"

  error.vm_dir_empty: VM directory is not set
  error.vm_dir_not_found: "VM directory does not exist: %s"
  error.vm_dir_access: "Cannot access VM directory: %s"
  error.vm_dir_not_dir: "VM path is not a directory: %s"
